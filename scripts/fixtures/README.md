# Fixtures for E2E tests

Optional: place `sample-cv.pdf` here to use a real CV in the E2E test 7 script instead of the auto-generated minimal PDF.

Otherwise use: `CV_PATH=/path/to/your/cv.pdf npx tsx scripts/e2e-test-7.ts`
